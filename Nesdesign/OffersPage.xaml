<Page x:Class="Nesdesign.OffersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Nesdesign"
      xmlns:models="clr-namespace:Nesdesign.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Zapytania">

    <Page.Resources>
        <DataTemplate x:Key="ButtonA">
            
            <Button Content="Stwórz zamówienie"
                Background="LightCoral"
                Click="CreateOrderCLick"  />
        </DataTemplate>


        <DataTemplate x:Key="ButtonB">
            <Button Content="{Binding orderNumber}"
                
                Click="OpenOrderFolderClick" />
        </DataTemplate>


        <DataTemplate x:Key="addProjectButton">
            <Button Content="Dodaj" x:Name="AddProjectButton"
                Background="DeepSkyBlue"
                Click="addProject"
                     Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridCell},
                          Path=Column.(local:MyTemplateColumn.ColumnKey)}"/>
        </DataTemplate>

        <DataTemplate x:Key="openFolderButton">
            <Button Content="{Binding projectPath}" x:Name="OpenProjectFolderButton"
            Background="LightGreen"
                     Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGridCell},
      Path=Column.(local:MyTemplateColumn.ColumnKey)}"
            Click="OpenOrderFolderClick"/>
        </DataTemplate>
        
        
        <local:OfferButtonTemplateSelector
        x:Key="OfferButtonSelector"
        TemplateA="{StaticResource ButtonA}"
        TemplateB="{StaticResource ButtonB}" />
        
       <local:ProjectButtonTemplateSelector
        x:Key="ProjectButtonSelector"
        TemplateA="{StaticResource addProjectButton}"
        TemplateB="{StaticResource openFolderButton}" />

        
        

    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Visible">
        <Grid ScrollViewer.CanContentScroll="True"  ScrollViewer.HorizontalScrollBarVisibility="Visible">
            <Label Content="{Binding SelectedOfferId}"></Label>
            <DataGrid x:Name="OffersDataGrid" Margin="0,20,10,10" ItemsSource="{Binding Offers}" CanUserAddRows="False" AutoGenerateColumns="False" CanUserSortColumns="True" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <RadioButton
                      
                        GroupName="SelectRow"
                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected, Mode=TwoWay}"
                               />


                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Nr oferty / zapytania" Binding="{Binding offerId}"  Width="Auto"/>
                    <DataGridTemplateColumn Header="Zdjęcie">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="80" Height="80" Click="OpenImage">
                                    <Image Stretch="Uniform"
                                    Source="{Binding Photo}"/>
                                </Button>
                                
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Opis"  Width="Auto" Binding="{Binding description}"/>
                    <DataGridTextColumn Header="Ilość"  Width="Auto" Binding="{Binding quantity}"/>
                    <DataGridTextColumn Header="Nazwa / numer"  Width="Auto" Binding="{Binding name}"/>
                    <DataGridTextColumn Header="Klient"  Width="Auto"/>
                    <DataGridComboBoxColumn Header="Status"  Width="Auto"  MinWidth="150" SelectedValueBinding="{Binding status, Mode=TwoWay}" SelectedValuePath="Value"
                                DisplayMemberPath="Description"
                                        ItemsSource="{Binding Source={x:Static models:OfferStatusEnumValues.AllValues} }"/>

                    
                    <DataGridTemplateColumn Header="Nr zamówienia"
                                CellTemplateSelector="{StaticResource OfferButtonSelector}" />



                    <local:MyTemplateColumn Header="Konstrukcja"
                 CellTemplateSelector="{StaticResource ProjectButtonSelector}"  ColumnKey="construction"/>

                    <local:MyTemplateColumn Header="Produkcja"
                 CellTemplateSelector="{StaticResource ProjectButtonSelector}" ColumnKey="production"/>
                   
                    <DataGridTextColumn Header="Kto" Binding="{Binding who}" Width="Auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Termin wykonania" Binding="{Binding date1}" Width="Auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Termin zamówienia" Binding="{Binding date2}" Width="Auto"></DataGridTextColumn>
                    <DataGridTemplateColumn Header="Zamykanie">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <CheckBox IsChecked="{Binding Closed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Text="ZAMKNIJ PROJEKT" Margin="5,0,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>

                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Closed}" Value="True"/>
                                    <Condition Binding="{Binding status}" Value="{x:Static models:OfferStatus.ZAKONCZONA}"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" Value="#555555"/>


                            </MultiDataTrigger>

                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
            <Border x:Name="Overlay"
                Background="#80000000"
                Visibility="Collapsed">

                <local:NewOrder x:Name="FormPanel"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            </Border>


        </Grid>
       

    </ScrollViewer>
    
</Page>
