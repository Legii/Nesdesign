<Page x:Class="Nesdesign.OffersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Nesdesign"
      xmlns:models="clr-namespace:Nesdesign.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Zapytania">

    <Grid>
        <DataGrid x:Name="OffersDataGrid" ItemsSource="{Binding Offers}" CanUserAddRows="False" AutoGenerateColumns="False" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr oferty / zapytania" Binding="{Binding offerId}"  Width="Auto"/>
                <DataGridTextColumn Header="Foto"  Width="Auto" />
                <DataGridTextColumn Header="Opis"  Width="Auto" Binding="{Binding description}"/>
                <DataGridTextColumn Header="Ilość"  Width="Auto" Binding="{Binding quantity}"/>
                <DataGridTextColumn Header="Nazwa / numer"  Width="Auto" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Klient"  Width="Auto"/>
                <DataGridComboBoxColumn Header="Status"  Width="Auto"  MinWidth="150" SelectedValueBinding="{Binding status, Mode=TwoWay}" SelectedValuePath="Value"
                                DisplayMemberPath="Description"
                                        ItemsSource="{Binding Source={x:Static models:OfferStatusEnumValues.AllValues} }"/>
                <DataGridTemplateColumn Header="Zamykanie">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox IsChecked="{Binding Closed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Text="ZAMKNIJ PROJEKT" Margin="5,0,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Closed}" Value="True"/>
                                <Condition Binding="{Binding status}" Value="{x:Static models:OfferStatus.ZAKONCZONA}"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Background" Value="#555555"/>
                          
                          
                        </MultiDataTrigger>

                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</Page>
